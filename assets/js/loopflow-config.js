// LoopFlow frontend configuration.
//
// Set this to your deployed Cloudflare Worker URL (the backend), e.g.
//   window.LOOPFLOW_API_BASE = "https://loopflow-backend.your-subdomain.workers.dev";
//
// Leave it as "" to run in OFFLINE mode: the app falls back to localStorage and
// the built-in sample feed (no login, no server persistence). This lets the
// site work before the backend is wired up.
//
// After deploying the Worker, paste its URL below and redeploy the frontend.
window.LOOPFLOW_API_BASE = "https://loopflow-backend.csasoorya10.workers.dev";
